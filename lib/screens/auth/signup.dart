import 'package:flutter/material.dart';

class SignupPgae extends StatelessWidget {
  const SignupPgae({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
